[project]
name = "yeo"
version = "0.3.1a1"
description = "A declarative dotfiles snapshot tool"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
yeo = "yeo.main:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
